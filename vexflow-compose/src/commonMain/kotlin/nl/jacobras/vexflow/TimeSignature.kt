package nl.jacobras.vexflow

class TimeSignature(timeSpec: String, customPadding: Int?) : StaveModifier(category = Category.TimeSignature) {

    override fun drawStave(stave: Stave, x: Int) {
        TODO("Not yet implemented")
    }
}